export default function PlaceholderPage({ title }) {
  return (
    <header className="page-header">
      <h1>{title}</h1>
      <p>Esta sección es un marcador de posición por ahora.</p>
    </header>
  )
}
